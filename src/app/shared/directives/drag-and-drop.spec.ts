import { DragAndDrop } from './drag-and-drop';

describe('DragAndDrop', () => {
  it('should create an instance', () => {
    const directive = new DragAndDrop();
    expect(directive).toBeTruthy();
  });
});
